doctype html
html(lang="pl")
    head
        title= title
        script(type='text/javascript').
            if (foo) {
                bar(1 + 5)
            }
    body
        #content
            include same_dir_one
        #sub
            include subdir/sub_dir_one